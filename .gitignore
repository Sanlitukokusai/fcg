.DS_Store
Thumbs.db
*.log

# Heavy video assets — GitHub rejects >100MB files. Host these on a CDN
# (Cloudflare R2 / Vimeo / etc.) and pass the URL via fcgSetVideo() in prod.
video/*.mp4
video/*.webm
video/*.mov
